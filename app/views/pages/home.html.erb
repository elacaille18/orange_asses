<%= render "shared/navbar_home"%>
<div class="banner" style="background-image: linear-gradient(-225deg, rgba(0,101,168,0.6) 0%, rgba(0,36,61,0.6) 50%), url(<%= image_path 'home.jpg'%>);">
  <div class="banner-content">
    <h1>Development Center</h1>
    <p>Welcome on Jouve & Associés assessments platform</p>
    <% if user_signed_in? %>
      <%= link_to "My dashboard", class:"btn btn-danger btn-lg"%>
    <%end%>
  </div>
</div>
